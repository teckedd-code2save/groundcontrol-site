# GroundControl marketing media

The public site uses real product captures only. Do not add invented dashboards, fake metrics, synthetic terminal output, or decorative UI mockups presented as product state.

## Current capture set — September 2026

The visible marketing page now uses captures synced directly from the current GroundControl product repository:

- `current-dashboard.png`
- `current-containers.png`
- `current-terminal.png`
- `current-topology.png`
- `current-infrastructure.png`

These replace the August product screenshots in the visible page. Older files may remain in Git history, but should not be reintroduced unless they still match the current product.

## Recent product evidence worth capturing next

1. **ChatGPT + GroundControl MCP**
   - connected custom app
   - scoped actions visible
   - live deployment health result

2. **Durable redeploy**
   - operation id returned from ChatGPT
   - GroundControl verification evidence
   - successful public HTTP verification

3. **Connector capability health**
   - GitHub source/read/webhook/GHCR capabilities
   - Daytona sandbox lifecycle + exact-revision acceptance

4. **Agent-assisted installation**
   - structured `--json` installer output
   - one-time claim page
   - post-claim private operator login
   - eventual HTTPS/domain setup

## Editing rules

- Prefer current real screenshots and short recordings over illustrations.
- Crop for focus, never to manufacture a different product state.
- Redact secrets rather than replacing them with fictional values.
- Never fabricate success states, counts or metrics.
- Keep typography and UI untouched inside screenshots.
- Replace visible captures when the product surface materially changes.
